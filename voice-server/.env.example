PORT=3001

# HMAC secret for voice tokens (generate with: openssl rand -hex 32)
# Must match VOICE_SECRET in the PHP app's .env
VOICE_SECRET=

# MySQL credentials (must match the PHP app's .env)
DB_HOST=localhost
DB_NAME=rank_chat_app
DB_USER=root
DB_PASS=
